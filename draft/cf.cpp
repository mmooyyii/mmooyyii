#include <iostream>
#include "set"
#include "string"
#include <bits/stdc++.h>

using namespace std;



bool isPrime(int n){
    for(int i = 1;i<n;i++){
        if(n%i==0){
            return false;
        }
    }
}

int main() {
    int t,n;
    scanf("%d", &t);
    while (t--){

    }
}